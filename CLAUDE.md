# CLAUDE.md — 저장소 루트

## 이 저장소
1. **유튜브 자막 자동 요약 도구** (`src/summarize.py`) — 개인 PC에서 실행. 자세한 내용은 `README.md`
2. **업무 위키** (`obsidian-wiki/`) — AI 에이전트 공용 장기 기억

## ⚡ 세션 시작 시 반드시 할 일

**`obsidian-wiki/CLAUDE.md` 를 읽으세요.** 위키 관련 모든 규칙의 정본입니다.

## 등록된 한국어 명령어

사용자가 아래 문장을 말하면, `obsidian-wiki/CLAUDE.md` 의 해당 명령을 실행합니다.

| 사용자가 말하면 | 실행할 명령 | 핵심 동작 |
|---|---|---|
| **"옵시디언 참조"** / "위키 참조해줘" / "맥락 불러와줘" | `reference` | index → log → 관련 wiki 노트를 읽고 **요약 보고 후** 작업 시작 (읽기 전용) |
| **"이번 작업 내용 옵시디언에 저장해줘"** / "옵시디언 저장" | `save` | **5-필터** 적용 → 통과분만 wiki 승격, 나머지는 conversations |
| **"이 자료 원본으로 넣어줘"** / "raw에 넣어줘" | `ingest` | 원문 그대로 `raw/` 저장. 자동 승격 금지 |
| **"위키에서 찾아줘"** / "전에 뭐라고 정했지?" | `query` | wiki → raw → conversations 순 검색. **출처 경로 함께 제시** |
| **"옵시디언 점검해줘"** / "린트 돌려줘" | `lint` | 추측·중복·무출처·노후 규칙 점검. **보고만, 자동수정 금지** |

## 절대 지킬 것

1. `obsidian-wiki/raw/` 파일 **수정·삭제 금지**
2. **5-필터를 통과하지 못한 내용은 `wiki/` 에 저장 금지**
3. 검증 안 된 추측은 `> 추정입니다` 를 붙여 `conversations/` 에만
4. 출처(`source`) 없는 결정은 `wiki/` 저장 금지
5. 사용자 승인 없이 `wiki/` 에 쓰지 않는다

## 답변 스타일
- 결론 먼저 → 쉬운 말(어려운 용어는 괄호로 뜻 설명) → 중간 길이
- 사실과 추측을 구분하고, 불확실하면 **"추정입니다"** 표시
- 친근한 존댓말, 호칭은 **"Chef Park"**
- 자세한 기준: `obsidian-wiki/wiki/90-공통규칙/글쓰기-톤앤매너.md`
