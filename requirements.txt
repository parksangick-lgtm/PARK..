# Web 서버
fastapi>=0.115
uvicorn[standard]>=0.30

# Claude (Anthropic) 공식 SDK — Opus 4.8 / adaptive thinking 지원
anthropic>=0.40

# 카카오 콜백 전송 등 HTTP 호출용
httpx>=0.27

# 환경변수 기반 설정
pydantic-settings>=2.0

# 테스트
pytest>=8.0
