# 유튜브 자동 요약 — 내 PC에서 쓰기

유튜브 주소만 넣으면 자막을 가져와 한국어 요약을 만들어 줍니다.

> GitHub 서버에서는 유튜브가 접속을 차단해 동작하지 않습니다. **내 PC에서 실행**하세요.

---

## 처음 한 번만 하는 준비 (약 10분)

### 1. 파이썬 설치

https://www.python.org/downloads/ 에서 **Download Python** 클릭 → 설치.

> ⚠️ 설치 첫 화면에서 **`Add python.exe to PATH`** 체크박스를 꼭 켜세요. 이걸 놓치면 실행이 안 됩니다.

이미 설치돼 있다면 넘어가세요.

### 2. 이 폴더 내려받기

GitHub 저장소 페이지 → 초록색 **Code** 버튼 → **Download ZIP** → 압축 풀기.

바탕화면 등 찾기 쉬운 곳에 두세요.

### 3. API 키 넣기

1. 폴더 안의 **`.env.example`** 파일을 복사합니다
2. 복사본 이름을 **`.env`** 로 바꿉니다 (앞의 점까지 포함)
3. 메모장으로 열어서 `sk-ant-...` 자리에 본인 키를 붙여넣고 저장

```
ANTHROPIC_API_KEY=sk-ant-api03-실제키
```

> `.env` 파일은 남에게 보내거나 GitHub에 올리지 마세요. 키가 유출되면 남이 내 돈으로 API를 씁니다.

---

## 매번 쓰는 법

1. 폴더 안의 **`실행하기.bat`** 을 더블클릭 (맥은 `실행하기.command`)
2. 유튜브 주소를 붙여넣고 **Enter**
3. 30초쯤 기다리면 완료

결과는 **`summaries` 폴더**에 날짜별 파일로 저장됩니다.

> 맨 처음 실행할 때만 필요한 프로그램을 자동으로 내려받느라 2~3분 걸립니다. 두 번째부터는 빠릅니다.

---

## 요약에 담기는 것

- 한 줄 요약
- 핵심 포인트 5~8개
- 상세 요약
- 인상적인 문장
- 다음 행동 / 시사점

형식을 바꾸고 싶으면 `src/summarize.py` 파일 안의 `PROMPT` 부분을 고치면 됩니다.

---

## 옵시디언에 바로 저장하기 (선택)

옵시디언을 쓰신다면, 요약을 **옵시디언 노트로 바로** 넣을 수 있습니다.

### 왜 되나요?

옵시디언 "볼트"는 특별한 프로그램 전용 파일이 아니라 **`.md` 파일이 들어 있는 그냥 폴더**입니다.
그래서 그 폴더에 파일 하나를 써 넣으면 옵시디언에 새 노트로 바로 나타납니다.

### 준비 (한 번만)

1. 옵시디언을 켜고 **설정 → 정보(About)** 에서 볼트 폴더 경로를 확인합니다
   (또는 볼트 폴더를 탐색기에서 열고 주소창을 복사)
2. `.env` 파일을 메모장으로 열어 맨 아래에 이렇게 적습니다

```
OBSIDIAN_VAULT=C:\Users\내이름\Documents\내볼트
```

### 쓰는 법

명령창에서 `--obsidian` 을 붙이면 됩니다.

```
python src\summarize.py "유튜브주소" --obsidian
```

볼트 안 **`유튜브 요약`** 폴더에 `2026-09-05 영상제목.md` 같은 이름으로 저장되고,
노트 맨 위에 제목·날짜·태그(`유튜브`, `요약`)·원본 링크가 자동으로 붙습니다.

요약이 아닌 다른 글(대본, 메모 등)을 저장하고 싶으면:

```
python src\obsidian_save.py --title "노트 제목" --folder "쇼츠 대본" --file 내용.md
```

> 옵시디언 앱이 꺼져 있어도 저장됩니다. 앱을 켜면 새 노트가 보입니다.

---

## 문제가 생겼을 때

| 화면에 나오는 말 | 뜻과 해결 |
|---|---|
| `파이썬이 설치되어 있지 않습니다` | 1번 단계를 안 했거나, 설치할 때 `Add python.exe to PATH` 를 안 켠 경우. 파이썬을 지우고 체크해서 다시 설치 |
| `.env 파일이 없습니다` | 3번 단계를 안 한 경우 |
| `ANTHROPIC_API_KEY 가 없습니다` | `.env` 안에 키가 안 들어갔거나 오타 |
| `credit balance is too low` | console.anthropic.com → Billing 에서 크레딧 충전 필요 |
| `자막이 없습니다` | 그 영상에 자막이 없음. 다른 영상으로 시도 |
| `유튜브가 현재 IP를 차단했습니다` | 잠시 뒤 재시도. 여러 개를 연속으로 돌리면 가끔 발생 |
| `옵시디언 볼트 경로를 모르겠습니다` | `.env` 에 `OBSIDIAN_VAULT=` 경로를 안 넣은 경우 |
| `볼트 폴더가 없습니다` | 경로에 오타가 있거나 폴더를 옮긴 경우. 경로를 다시 확인 |

---

## 비용

영상 1편당 대략 20~50원입니다. $5(약 7천원) 충전하면 100편 넘게 쓸 수 있습니다.

## 다른 언어 자막

기본은 한국어 → 영어 순으로 찾습니다. 일본어 영상 등은 명령창에서:

```
python src\summarize.py "주소" --lang ja,en
```
